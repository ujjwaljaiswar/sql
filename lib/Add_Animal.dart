import 'package:flutter/material.dart';

class AddAnimalPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Add Animal'),
      ),
      body: Center(
        child: Text('This is the Add Animal Page'),
      ),
    );
  }
}

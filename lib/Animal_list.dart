import 'package:flutter/material.dart';

class AnimalListPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Animal List'),
      ),
      body: Center(
        child: Text('This is the Animal List Page'),
      ),
    );
  }
}
